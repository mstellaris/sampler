services:
  backend:
    build: ./backend
    env_file: ./.env
    volumes:
      - bookmarks-data:/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "9090:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  bookmarks-data:
